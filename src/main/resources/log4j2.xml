<?xml version="1.0" encoding="UTF-8"?>
<Configuration xmlns="https://logging.apache.org/xml/ns"
               xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
               xsi:schemaLocation="
                   https://logging.apache.org/xml/ns
                   https://logging.apache.org/xml/ns/log4j-config-2.xsd">
    <Appenders>
        <!-- Logs to console -->
        <Console name="CONSOLE">
            <PatternLayout pattern="%p - %m%n"/>
            <!--Alternate Pattern = "<PatternLayout pattern="%d [%t] %p %c - %m%n"/">-->
        </Console>

        <!-- This is our primary location for logging -->
        <File name="MAIN" fileName="logs/main.log">
            <PatternLayout pattern="%p - %m%n"/>
        </File>

        <File name="ERRORS" fileName="logs/errors.log">
            <PatternLayout pattern="%p - %m%n"/>
        </File>
    </Appenders>

    <!-- Here's where we set logging  -->
    <Loggers>
        <Logger name="com.PhamKornbluhGroup" level="ALL">
            <AppenderRef ref="CONSOLE" level="ALL"/>
            <AppenderRef ref="MAIN" level="ALL"/>
            <AppenderRef ref="ERRORS" level="ERROR"/>
        </Logger>
        <Root level="ERROR">
            <AppenderRef ref="CONSOLE" level="ALL"/>
            <AppenderRef ref="MAIN" level="ALL"/>
            <AppenderRef ref="ERRORS" level="ERROR"/>
        </Root>
    </Loggers>
</Configuration>